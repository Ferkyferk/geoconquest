// Re-exports used only in the HowToPlay UI so it displays accurate numbers.
// These must match the values in engine.ts / scoring.ts / TriviaPanel.tsx.
export const PASS_THRESHOLD = 3;
export { STARTING_LIVES } from '@/lib/game/scoring';
